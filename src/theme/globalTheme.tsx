import {StyleSheet} from 'react-native';

export const globalTheme = StyleSheet.create({
  goBackButton: {
    position: 'absolute',
    top: 20,
    left: 20,
  },
  buttons: {
    borderRadius: 20,
    padding: 10,
    marginTop: 10,
  },
});
